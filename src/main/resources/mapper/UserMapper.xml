<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="jp.wits.dev.mapper.UserMapper">

    <resultMap
        id="userResultMap"
        type="jp.wits.dev.model.entity.User">

        <id
            property="id"
            column="user_id" />

        <result
            property="email"
            column="email" />

        <result
            property="passwordHash"
            column="password_hash" />

        <result
            property="displayName"
            column="display_name" />

        <result
            property="enabled"
            column="enabled" />

        <result
            property="accountLocked"
            column="account_locked" />

        <result
            property="failedLoginAttempts"
            column="failed_login_attempts" />

        <result
            property="lockedAt"
            column="locked_at" />

        <result
            property="lastLoginAt"
            column="last_login_at" />

        <result
            property="passwordChangedAt"
            column="password_changed_at" />

        <result
            property="createdAt"
            column="created_at" />

        <result
            property="updatedAt"
            column="updated_at" />

        <collection
            property="roles"
            ofType="jp.wits.dev.model.entity.Role">

            <id
                property="id"
                column="role_id" />

            <result
                property="roleCode"
                column="role_code" />

            <result
                property="roleName"
                column="role_name" />

        </collection>

    </resultMap>


    <select
        id="findByEmail"
        resultMap="userResultMap">

        SELECT
            u.id AS user_id,
            u.email,
            u.password_hash,
            u.display_name,
            u.enabled,
            u.account_locked,
            u.failed_login_attempts,
            u.locked_at,
            u.last_login_at,
            u.password_changed_at,
            u.created_at,
            u.updated_at,
            r.id AS role_id,
            r.role_code,
            r.role_name
        FROM users AS u
        LEFT JOIN user_roles AS ur
            ON ur.user_id = u.id
        LEFT JOIN roles AS r
            ON r.id = ur.role_id
        WHERE LOWER(u.email) = LOWER(#{email})
        ORDER BY r.id

    </select>

</mapper>
