spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/nuts_app}
    username: ${DB_USERNAME:nuts_app}
    password: ${DB_PASSWORD:local_password}
    driver-class-name: org.postgresql.Driver
